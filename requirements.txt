# ─── XtreamRip ────────────────────────────────────────────────────────────────
# Core dependencies — install with: pip install -r requirements.txt

# HTTP requests (API calls + stream downloads)
requests>=2.31.0

# Beautiful terminal output, panels, tables, and progress bars
rich>=13.7.0

# Interactive CLI prompts (menus, checkboxes, password input)
questionary>=2.0.1

# ─── Discord Bot (optional) ──────────────────────────────────────────────────
# Required only for the Discord bot integration (coming soon)
# Install separately: pip install discord.py aiohttp

# discord.py>=2.3.0
# aiohttp>=3.9.0
